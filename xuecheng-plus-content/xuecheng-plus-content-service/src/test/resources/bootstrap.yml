spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: 121.40.138.25:8848
      discovery:
        namespace: dev
        group: xuecheng-plus-project
      config:
        namespace: dev
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
        extension-configs:
          - data-id: content-service-${spring.profiles.active}.yaml
            group: xuecheng-plus-project
            refresh: true

  #profiles默认为dev
  profiles:
    active: dev